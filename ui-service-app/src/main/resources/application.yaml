server:
  port: 8083

spring:
  application:
    name: ui-service-app
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
