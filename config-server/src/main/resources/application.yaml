server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "file:///${user.home}/IdeaProjects/distributed-tac-toe/configuration/src/main/resources"
